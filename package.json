{
  "name": "bitrix24",
  "version": "1.5.0",
  "main": "main.js",
  "description": "Контроль рабочего времени",
  "author": "Скибин А.Д.",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . --overwrite --asar --platform=win32 --arch=x64 --prune=true --out=dist --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"bitrix24\""
  },
  "dependencies": {
    "axios": "latest",
     "moment": "^2.30.1"
  },
  "devDependencies": {
    "electron": "latest",
    "electron-packager": "latest"
  }
}
